pipeline {
    agent any

    stages {
        stage('Linux Test') {
            steps {
                sh 'hostname'
                sh 'pwd'
                sh 'echo "Hello from GitHub Jenkinsfile"'
            }
        }
    }
}
